import { SupervisorDashboardStats } from './SupervisorDashboardStats';

/**
 * Wrapper component for supervisor statistics
 * Uses the SupervisorDashboardStats component with real data
 */
export function SupervisorStats() {
  return <SupervisorDashboardStats />;
}
